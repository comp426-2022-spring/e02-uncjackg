{
  "tests": [
    {
      "name": "Build and run",
      "setup": "npm install && npm run build && npm install -g serve",
      "run": "(timeout --signal=SIGINT 5 serve -s build; exit 0)",
      "input": "",
      "output": "INFO: Accepting connections",
      "comparison": "included",
      "timeout": 10,
      "points": 25000
    }
  ]
}